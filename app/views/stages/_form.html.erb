<div class="app_form">

	<%= form_for(@stage) do |f| %>
  <% if @stage.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@stage.errors.count, "error") %> prohibited this stage from being saved:</h2>

    <ul>
      <% @stage.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <fieldset>
		<legend>D&eacute;tails du stage</legend>
		
		<div class="field">
			<%= f.label :title, "Titre" %><br />
			<%= f.text_field :title %>
		</div>
		<div class="field">
			<%= f.label :start, "Debut" %><br />
			<%= f.date_select :start %>
		</div>
		<div class="field">
			<%= f.label :end, "Fin" %><br />
			<%= f.date_select :end %>
		</div>
		<div class="actions">
			<%= f.submit %>
		</div>
	</fieldset>
	
	<% end %>

</div>
