<% content_for :title do "Edition de l'inscription" end %>
<% content_for :subtitle do %>
  <%= "#{@inscription.adherent.nom} #{@inscription.adherent.prenom} - #{@inscription.stage.title}" %>
<% end %>

<%= render partial: 'form', locals: {mode: :update} %>

<br>
<%= link_to 'Back', :back %>
